# The Dune API key is NOT read from here. Each visitor pastes their own key
# into the in-app panel; it lives only in their browser's localStorage.
#
# This file only carries the (non-secret) saved Dune query ID baked in at
# build time. The default in src/config.js already points at the published
# query, so this override is optional.

# Saved query: "AutoRange (reCLAMM) — LP vs HODL daily state (Ethereum)"
# https://dune.com/queries/7649043
VITE_DUNE_QUERY_ID=7649043
